# Шаблон окружения production (infrastructure).
# Перед install: cp -n "$INFRA_ROOT/prod.env" "$INFRA_ROOT/.env"

DEPLOY_MODE=prod

# --- 1. Пути, корни, instance, БД (для compose см. *.test.env) ---
OPTIONS_FILE=/etc/autoteka/options.env
AUTOTEKA_ROOT=/opt/autoteka/
INFRA_ROOT=/opt/autoteka/infrastructure/
RUN_INSTANCE=autoteka
# DB_CONNECTION=sqlite
# DB_DATABASE=

# --- 2. Сеть (веб: bind и порт на хосте; внутри контейнера nginx — 80) ---
HTTP_BIND_IP=0.0.0.0
HTTP_PORT=80
# Пример prod: HTTP_BIND_IP=0.0.0.0, HTTP_PORT=80

# --- 3. Smoke и таймауты ---
PHP_READY_TIMEOUT=60
ADMIN_HEALTH_URL=http://127.0.0.1/admin/login
BACKEND_HEALTH_URL=http://127.0.0.1/up
API_HEALTH_URL=http://127.0.0.1/api/v1/category-list
HEALTH_RETRY=20
HEALTH_DELAY=2

# --- 4. Сборка образов ---
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# --- 5. Laravel в контейнере php ---
LARAVEL_OPTIMIZE=true

# --- 6. PHP-FPM и OpCache ---
PHP_FPM_PM_MAX_CHILDREN=20
PHP_FPM_PM_START_SERVERS=4
PHP_FPM_PM_MIN_SPARE_SERVERS=2
PHP_FPM_PM_MAX_SPARE_SERVERS=6
PHP_OPCACHE_VALIDATE_TIMESTAMPS=0
PHP_OPCACHE_REVALIDATE_FREQ=0

# --- 7. Healthcheck сервиса php в Compose ---
PHP_HC_INTERVAL=30s
PHP_HC_TIMEOUT=5s
PHP_HC_RETRIES=3
PHP_HC_START_PERIOD=450s

# --- 8. Сервис web: сборка фронта и кэш API ---
VITE_API_BASE_URL=/api/v1
VITE_BUILD_SOURCEMAP=false

API_CACHE_TTL=3600
# API_CACHE_CONTROL_GET=
# API_CACHE_CONTROL_MUTATION=

# --- 9. Upstream Vite и CORS ---
# FRONTEND_MODE=source
# FRONTEND_PORT=5173
# FRONTEND_UPSTREAM_HOST=frontend
# FRONTEND_UPSTREAM_PORT=
# FRONTEND_HMR_PROTOCOL=ws
# FRONTEND_HMR_HOST=localhost
# FRONTEND_HMR_PORT=8081

# CORS_ALLOW_ORIGIN=*
# CORS_ALLOW_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
# CORS_ALLOW_HEADERS=Authorization,Content-Type,Accept,Origin,X-Requested-With
# CORS_EXPOSE_HEADERS=Content-Length,Content-Range
# CORS_ALLOW_CREDENTIALS=false
# CORS_MAX_AGE=86400

# --- 10. Xdebug (dev php) ---
# XDEBUG_MODE=debug,develop
# XDEBUG_START=yes
# XDEBUG_CLIENT=host.docker.internal
# XDEBUG_PORT=9003
# XDEBUG_IDEKEY=VSCODE

# --- 11. Git: watch-changes (хост) ---
GIT_BRANCH=master
GIT_REMOTE=origin

# --- 12. Watchdog и health URL (хост) ---
WATCHDOG_LOAD_LIMIT=90
WATCHDOG_RAM_LIMIT=90
WATCHDOG_BOOT_GRACE=180
WATCHDOG_STAGE1_MAX_FAILS=2
WATCHDOG_STAGE2_MAX_FAILS=4
WATCHDOG_REBOOT_COOLDOWN=3600
WATCHDOG_NGINX_FAIL_THRESHOLD=2
WATCHDOG_PHP_FAIL_THRESHOLD=2
WATCHDOG_BACKEND_FAIL_THRESHOLD=2
WATCHDOG_ADMIN_FAIL_THRESHOLD=2
WATCHDOG_API_FAIL_THRESHOLD=2
WATCHDOG_NGINX_REPAIR_COOLDOWN=600
WATCHDOG_PHP_REPAIR_COOLDOWN=600
WATCHDOG_BACKEND_REPAIR_COOLDOWN=1800
WATCHDOG_ADMIN_REPAIR_COOLDOWN=1800
WATCHDOG_API_REPAIR_COOLDOWN=0
WATCHDOG_NGINX_MAX_REPAIRS=2
WATCHDOG_PHP_MAX_REPAIRS=2
WATCHDOG_BACKEND_MAX_REPAIRS=2
WATCHDOG_ADMIN_MAX_REPAIRS=2
WATCHDOG_API_MAX_REPAIRS=0
WATCHDOG_NGINX_REPAIR_VERIFY_TIMEOUT=75
WATCHDOG_PHP_REPAIR_VERIFY_TIMEOUT=75
WATCHDOG_CONTAINER_REPAIR_VERIFY_INTERVAL=3

# --- 13. Резервное копирование (хост) ---
BACKUP_DAYS=7

# --- 14. Логи, health state, temp ---
LOG_DIR=/var/log/autoteka
HEALTH_DIR=/var/lib/autoteka/health

# --- 15. Telegram (секреты после install уходят в TELEGRAM_ENV_FILE) ---
TELEGRAM_ENV_FILE=/etc/autoteka/telegram.env
TELEGRAM_TOKEN=
TELEGRAM_CHAT=

# --- 16. Прочее (Moonshine) ---
MOONSHINE_ADMIN_EMAIL=admin@example.com
