# CLAUDE.md

Конвенции для каждой свежей агентской сессии в этом репо.
Читай целиком перед первой правкой — процесс важнее скорости.

## 1. Коммиты

- Subject: `scope: краткое` по-русски, ≤50 символов.
- Тело: что и зачем, перенос строк вручную.
- Трейлер `Author: i-lkn` отдельной строкой в конце.
- БЕЗ `Co-Authored-By` и БЕЗ «Generated with …».
- Текст коммита присылает планировщик дословно — НЕ
  сочиняй свой. Расхождение присланного текста с
  реализацией = STOP (код под текст не подгонять).

Пример:

```text
scope: краткое описание

Тело: что сделано и почему.

Author: i-lkn
```

## 2. Стейдж только поимённо

- Перечисляй файлы явно: `git add path/a path/b`.
- `git add -A` и `git add -a` запрещены.

## 3. Гейт из `frontend/` перед каждым коммитом

Запускать из `frontend/`:

```bash
npx eslint .
npx vue-tsc --noEmit
npx stylelint "src/styles/*.css"
npm run test:ui:mock
```

- `test:ui:mock`: дефолтный вьюпорт 390×844; десктоп-кейсы
  ставят вьюпорт per-test (1280×800 / 1440×900).
- Гоняй сильнейшую прямую проверку изменённой поверхности,
  не только baseline.

## 4. Замороженный долг — НЕ чинить

Не чини и не считай за сигнал гейта. Фактический набор
(сверено 2026-06-10, перепроверяй — он дрейфует):

- vue-tsc: `GalleryCarousel.vue` ×2, `sortShops.spec.ts:9`.
- unit: `HttpApiClient.galleryItems.spec.ts` ×2.
- eslint (6 ошибок): `HttpApiClient.galleryItems.spec.ts`
  ×2, `HttpApiClient.promotion.spec.ts` ×2, `env.d.ts` ×2.
- prettier: `src/styles/tailwind.css` не отформатирован.

## 5. Канон = `_prototype/` (gitignored)

- Значения дизайна (цвета, отступы, радиусы, скейлы) бери
  ДОСЛОВНО из `_prototype/*.jsx`, не выдумывай.
- Нет значения в каноне или структурное расхождение = STOP
  и доклад, дизайн не изобретать.

## 6. Протокол визуального STOP

- Новый визуал → визуальный STOP перед коммитом: скрины на
  нужных вьюпортах/фикстурах, ждать ок Игоря.
- Gate-encodable правки (логика, токены, гарды) →
  самокоммит после зелёного гейта.

## 7. Живой отклик (требование заказчика №1)

Любой интерактив:

- `:active` — `scale(0.97)`; мелкие таргеты ≤44px — `0.92`.
- hover без роста (никакого увеличения).
- focus-visible — кольцо по спеке (`_prototype`).

## 8. Нейминг и стиль

- CSS-классы: `.shop-*` / `.catalog-*`.
- Цвета — токены `@theme`, хексы не хардкодить.
- Команды в терминал — однострочные.
