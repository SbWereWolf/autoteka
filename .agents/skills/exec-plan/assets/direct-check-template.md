# Direct checks for <task-slug>

## Changed surface

- `<path or subsystem>`

## Why the baseline gate is insufficient on its own

- <reason>

## Direct checks selected

- Command: `<command>`
  - Purpose: <what it proves>
  - Expected: <success signal>

## Result log

- `<YYYY-MM-DD HH:MM:SS UTC> — <command> — <exit/result>`
