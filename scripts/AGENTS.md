# Scripts local instructions

These instructions refine the root `AGENTS.md` for `scripts/`.

## Primary skill

Use `repo-tooling` for tasks centered on workflow mechanics.

## Rules

1. Changes here are workflow/tooling changes, not product behavior
   changes.
2. Validate changed scripts directly.
3. Do not claim success from cached app tests alone.
4. Keep commands copy-pasteable and platform-aware.
